{
  "name": "",
  "version": "",
  "description": "The `gulpfile.js` is a task runner to inline the css styles in a HTML email.",
  "devDependencies": {
    "glob": "^7.1.1",
    "gulp": "^3.9.1",
    "gulp-inline-css": "^3.1.0",
    "gulp-prettify": "^0.5.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "gulp",
    "build": "gulp",
    "gulp": "gulp"
  },
  "author": "Richard Jackson",
  "license": "ISC"
}
